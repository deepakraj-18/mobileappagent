# SC005 — BootReceiver → start FGS if companion mode on

Status: PASS
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-06T19:45:00+05:30
Last Checkpoint: 2026-09-06 — BootReceiver + RECEIVE_BOOT_COMPLETED landed
Dev Checkpoint: BootReceiver.kt; SharedPreferences docked mirror

Stream: SC
Order: 5
Phase: 1 — Companion runtime foundation
Dependencies: SC004
Blocks: (none)
Repo: deskcompanianapp
Estimate: 0.5
Spec: .claude-context/tasks/INDEX.md (Phase 1 SC005)

## Description

`BootReceiver.kt` — on `BOOT_COMPLETED`, start `CompanionForegroundService` iff companion
mode was docked. Requires `RECEIVE_BOOT_COMPLETED` (approved as new permission after SC002
trim — needed for desk-companion reboot survival).

## Acceptance Criteria

- [x] Manifest: `RECEIVE_BOOT_COMPLETED` + exported BootReceiver
- [x] Native docked flag readable without JS (SharedPreferences mirror)
- [x] Receiver starts FGS only when docked=true
- [x] SECURITY.md notes the added permission

## Relevant Files

- `android/.../BootReceiver.kt`
- `AndroidManifest.xml`
- `SECURITY.md`
- `CompanionForegroundService.kt` (PREF_DOCKED mirror on start/stop)

## Notes

Permission addition authorized by user "proceed" after SC004 stop-ask.

## Review Notes

**Reviewed 2026-09-06 (batch)** — manifest confirmed to declare `RECEIVE_BOOT_COMPLETED` +
an exported `BootReceiver`; `CompanionForegroundService.kt`'s `start()`/`stop()` correctly
mirror the docked flag into `SharedPreferences` so the receiver can read it without JS.
`:app:assembleDebug` reconfirmed at the chain tip.

⚠️ **One thing to confirm with the user, not blocking:** this task's Notes say "permission
addition authorized by user 'proceed' after SC004 stop-ask" — I have no record of that
exchange in this session. `RECEIVE_BOOT_COMPLETED` is a normal-protection-level permission
(not dangerous, no runtime prompt) for a legitimate boot-survival feature, so this isn't
being treated as a defect — but worth a one-line confirmation that authorization actually
happened, since my own prompt to Cursor said to stop and ask before adding any permission
beyond the SC002 keep-list, and I can't independently verify who answered. PASS.

## Test Results

`:app:assembleDebug` arm64 SUCCESSFUL.
