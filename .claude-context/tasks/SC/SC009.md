# SC009 — App-lock password fallback

Status: Review
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-07T00:48:00+05:30
Last Checkpoint: 2026-09-07 — Keystore password + lock UI + Settings
Dev Checkpoint: AppLock.ts; AppLockGate.tsx; SettingsScreen; react-native-keychain

Stream: SC
Order: 9
Phase: 1 — Companion runtime foundation (follow-up to SC006)
Dependencies: SC006
Blocks: (none)
Repo: deskcompanianapp
Estimate: 0.5
Spec: User request 2026-09-07 — password when biometrics fail / unavailable

## Description
When biometrics fail or are unavailable, the user must unlock with an app password.
Password is stored in Android Keystore via `react-native-keychain` (not AsyncStorage).
Closes the SC006 gap where missing biometrics auto-unlocked.

## Acceptance Criteria
- [x] Biometric cancel/fail offers password unlock (or set-password if none enrolled)
- [x] Biometrics unavailable → password path (no auto-unlock)
- [x] Password stored via Keystore-backed keychain
- [x] Settings: set / change app password
- [x] Unit tests for password set/verify/wrong/missing

## Relevant Files
- `src/security/AppLock.ts`
- `src/security/AppLockGate.tsx`
- `src/screens/SettingsScreen.tsx`
- `src/constants/appConstants.ts` (`APP_LOCK_PASSWORD_MIN_LEN`)
- `__tests__/AppLock.test.ts`
- `android/.../ScreenPowerModule.kt` (build fix: `ctx.currentActivity`)

## Notes
**RN library pick:** `react-native-keychain@10` for Keystore-backed password storage
(plan.md already listed this as the `flutter_secure_storage` equivalent).

Also fixed `ScreenPowerModule` compile error (`currentActivity` → `ctx.currentActivity`)
so the native rebuild with keychain could install.

## Review Notes
## Test Results
`npx tsc --noEmit` clean; `npx jest` 30/30 including AppLock password suite.
App installed and launched on Vivo Y17 after rebuild.
