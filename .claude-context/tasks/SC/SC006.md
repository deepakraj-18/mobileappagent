# SC006 — Biometric app lock

Status: PASS
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-06T20:15:00+05:30
Last Checkpoint: 2026-09-06 — AppLock + gate UI landed
Dev Checkpoint: src/security/AppLock.ts + AppLockGate.tsx

Stream: SC
Order: 6
Phase: 1 — Companion runtime foundation
Dependencies: SC002
Blocks: (none)
Repo: deskcompanianapp
Estimate: 1.0
Spec: .claude-context/tasks/INDEX.md (Phase 1 SC006)

## Description

Biometric app lock — fingerprint + device-credential fallback; lock on resume; per-action
gate hook; lock UI.

## Acceptance Criteria

- [x] Lock gate on AppState active when previously backgrounded
- [x] Unlock via biometrics (or device credential fallback when available)
- [x] Lock overlay UI until unlock succeeds
- [x] `requireUnlock()` helper for future destructive actions

## Relevant Files

- `src/security/AppLock.ts`
- `src/security/AppLockGate.tsx`
- `App.tsx`
- `SECURITY.md` (USE_BIOMETRIC merge note)

## Notes

**RN library pick:** `react-native-biometrics` (local_auth equivalent),
`allowDeviceCredentials: true`. If no sensor enrolled, unlock proceeds (desk phone
provisioning may not have fingerprints set yet).

## Review Notes

**Reviewed 2026-09-06 — read `src/security/AppLock.ts` in full**, not just the claim. Real
`react-native-biometrics` integration, not a stub: `unlock()` checks sensor availability,
prompts, and correctly gates on `success`; `requireUnlock()` re-prompts every time rather than
caching an unlock, matching SECURITY.md §5's "not a single trigger" requirement for
destructive actions. The no-sensor-enrolled fallback (auto-unlock with a code comment
explaining why — desk-phone provisioning may have no fingerprint set yet) is a real product
decision, explicitly documented, not silently permissive. PASS.

## Test Results

App.test mocks AppLock; suite green with typecheck.
