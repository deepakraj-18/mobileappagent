# FI005 — Wire Dock ↔ CompanionMode + WakeWord + VoiceSession

Status: PASS
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-07T00:10:00+05:30
Last Checkpoint: 2026-09-07 — DockVoiceRuntime + RootNavigator DockRoute
Dev Checkpoint: src/voice/DockVoiceRuntime.ts; src/navigation/RootNavigator.tsx

Stream: FI
Order: 5
Phase: 2 — Voice loop
Dependencies: FD001, BD004
Blocks: (Phase 3 FI030 for hub chip)
Repo: deskcompanianapp
Estimate: 0.75
Spec: .claude-context/tasks/INDEX.md (Phase 2 FI005)

## Description

Wire Dock ↔ CompanionMode + WakeWord + VoiceSession together. Hub and presence stay
unwired until their own phases land. (This is THIS REPO's FI005 — not the parent LifeOS
Health FI005.)

## Acceptance Criteria

- [x] Entering docked mode starts wake-word duty-cycle + voice session
- [x] Leaving docked mode stops them
- [x] Dock shows live WakeState from VoiceSession
- [x] Tap-to-talk triggers a voice session turn
- [x] Hub connection + presence chips remain placeholders

## Relevant Files

- `src/voice/DockVoiceRuntime.ts`
- `src/navigation/RootNavigator.tsx` (`DockRoute`)
- `__tests__/DockVoiceRuntime.test.ts`

## Notes

`DockRoute` mounts only while CompanionMode is DOCKED. `startDockVoiceRuntime()` binds
WakeWord duty-cycle, subscribes wake events → `VoiceSession.onWake()`, mirrors session
state into DockScreen. STT is `stubSttProvider` (empty capture) until a real STT task;
tap-to-talk and wake still drive the full state machine (empty → “didn't catch that” TTS).
Hub/presence chips unchanged (UNPAIRED / UNKNOWN defaults).

## Review Notes

**Reviewed 2026-09-07** — `DockVoiceRuntime.ts` (91 lines) genuinely wires CompanionMode ↔
WakeWord ↔ VoiceSession; hub/presence chips confirmed untouched. Honest about STT being a stub
(`stubSttProvider`, empty capture) rather than pretending real speech recognition exists yet.
PASS.

## Test Results

`npx tsc --noEmit` clean; `npx jest` all green including DockVoiceRuntime.
