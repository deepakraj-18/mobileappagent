# IF001 — CI (tsc + jest) + Firebase project wiring for FCM

Status: PASS
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-06T18:50:00+05:30
Last Checkpoint: 2026-09-06 — CI + placeholder google-services landed
Dev Checkpoint: typecheck+jest green; assembleDebug with google-services plugin OK

Stream: IF
Order: 1
Phase: 1 — Companion runtime foundation
Dependencies: (none)
Blocks: BD033 (FCM; later phase)
Repo: deskcompanianapp
Estimate: 0.75
Spec: .claude-context/tasks/INDEX.md (Phase 1 IF001)

## Description

GitHub Actions runs `tsc --noEmit` + `jest` (RN override of the Flutter analyze/test
wording). Also wire a Firebase Android app + `google-services.json` so Phase 3 FCM
(`BD033`) has a project to attach to — without installing the full messaging client yet.

## Acceptance Criteria

- [x] `.github/workflows/ci.yml` runs on push/PR: `npm ci`, `npx tsc --noEmit`, `npm test`
- [x] `package.json` scripts: `typecheck`, `test` (jest)
- [x] Jest suite passes in CI (native modules mocked / soft-linked for Node)
- [x] `android/app/google-services.json` present for `com.privateagent` + Google Services
      Gradle plugin applied
- [x] Notes record: placeholder vs real Firebase project owner action

## Relevant Files

- `.github/workflows/ci.yml`
- `package.json`
- `android/build.gradle` / `android/app/build.gradle`
- `android/app/google-services.json`
- `docs/FIREBASE.md`
- `src/native/Accessibility.ts` (test-safe)

## Notes

RN library picks: none beyond existing RN toolchain. Firebase Messaging JS SDK deferred to
BD033; this task only lands project config + Gradle plugin.

**Firebase:** `google-services.json` is a **placeholder** (`privateagent-fcm-placeholder`).
Owner must replace with a real console download before BD033 — see `docs/FIREBASE.md`.

## Review Notes

**Reviewed 2026-09-06 (batch, part of full Phase-1 chain review)** — `.github/workflows/ci.yml`
present and runs npm ci/typecheck/test; confirmed `google-services.json` exists and is
correctly labelled a placeholder in `docs/FIREBASE.md`, not silently passed off as real.
Re-ran `npx tsc --noEmit` and `npx jest` myself at the chain tip: clean / all green. PASS.

## Test Results

`npm run typecheck` OK; `npm test -- --ci` PASS (1 suite); `:app:assembleDebug` OK with
`com.google.gms.google-services`.
