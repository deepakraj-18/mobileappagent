# QA061 — Offline / degraded-mode testing

Status: Review
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-09T19:30:00+05:30
Last Checkpoint: implemented
Dev Checkpoint: scenario matrix + Qa061OfflineDegraded suite

Stream: QA
Order: 61
Phase: 6 — Onboarding & hardening
Dependencies: Phase 3–5
Blocks: (none)
Repo: deskcompanianapp
Estimate: 1.0
Spec: INDEX QA061

## Description

Verify offline / degraded behaviour: brain unreachable, internet down, token expired —
local fallback LLM, BLE presence, outbox replay, stale cards, status messaging.

## Acceptance Criteria

- [x] Scenario matrix documented with expected UX/code behaviour
- [x] Automated coverage mapped + gaps filled where cheap
- [x] Status messaging (Dock banner / chips / stale cards) verified in tests
- [x] Status Review (never PASS)

## Relevant Files

- `docs/QA061_OFFLINE_DEGRADED.md`
- `__tests__/Qa061OfflineDegraded.test.tsx`

## Notes

Manual airplane-mode soak / Keystore-forced expiry left as operator checklist.
Degraded banner requires configured local fallback provider.

## Review Notes

Independently verified 2026-09-09: full-repo rerun green (see FD060 notes). Read
`Qa061OfflineDegraded.test.tsx` directly: it exercises real production classes
(`DegradedMode`, `HubAuth`, `PresenceService`, `HubOutbox`, `DockCardsController`, and a
rendered `DockScreen`) with specific-value assertions per scenario (degraded-banner testID
present, connection chip text matches /offline/i, token actually rotates on refresh, outbox
`replayPending()` returns `{sent:1,failed:0}`, stale cards report a real `staleSince`) — not
vacuous renders. Manual airplane-mode soak and forced Keystore token expiry are correctly
left as an operator checklist rather than claimed done. **Leaving at `Review` per this task's
own AC ("Status Review (never PASS)") for the same reason as QA060 — real manual work still
outstanding.**

## Test Results

- `npx tsc --noEmit` — clean
- `npx jest __tests__/Qa061OfflineDegraded.test.tsx` — 6/6 pass
