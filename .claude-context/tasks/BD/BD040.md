# BD040 — Hub-driven TaskExecutor think step

Status: PASS
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-09T15:35:00+05:30
Last Checkpoint: implementation complete
Dev Checkpoint: TaskStep + TaskExecutor hub think loop; 7 tests green

Stream: BD
Order: 40
Phase: 4 — Agent execution via hub
Dependencies: BD032
Blocks: BD041, BD042
Repo: deskcompanianapp
Estimate: 2.0
Spec: hub-contract.md §8.2; INDEX BD040

## Description

Refactor TaskExecutor "think" step → request agent_action from the hub; stream
AGENT_SCREEN / AGENT_STEP_RESULT (+ screenshot) back; keep TaskStep.tryParse contract.

## Acceptance Criteria

- [x] TaskStep.tryParse preserves Flutter wire vocabulary (+ hub SCREAMING_SNAKE normalize)
- [x] Think step emits AGENT_SCREEN and waits for AGENT_ACTION (MockHubClient in tests)
- [x] Executes exactly one action per AGENT_ACTION via Accessibility bridge
- [x] Emits AGENT_STEP_RESULT after each action
- [x] Unit tests green with MockHubClient

## Relevant Files

- `src/agent/TaskStep.ts`
- `src/agent/TaskExecutor.ts`
- `src/agent/index.ts`
- `__tests__/TaskStep.test.ts`
- `__tests__/TaskExecutor.test.ts`

## Notes

RN had no TaskExecutor yet (Flutter archive). Built hub-driven think from day one rather than
porting local-LLM think then ripping it out. `open_app` returns a note until a launcher bridge
exists. Pending AGENT_ACTION is registered **before** AGENT_SCREEN to avoid reply races.

## Review Notes

Independently verified 2026-09-09 (reviewer, not the implementer's self-report): re-ran
`npx tsc --noEmit` and `npx jest` for the **whole repo**, not just the files this task
claims — 24/24 suites, 65/65 tests green, matching the sum of every Phase 4 task's individual
claim (guard-tests.md rule #4: never trust a per-task count without confirming the whole
build). Read `GoalCommandRouter.ts`/`TaskExecutor.ts` directly: AGENT_ACTION is registered
before AGENT_SCREEN is emitted (avoids the reply race called out in Notes), and
`TaskStep.tryParse` normalizes hub SCREAMING_SNAKE into the existing Flutter vocabulary rather
than replacing it. **PASS.**

## Test Results

- `npx tsc --noEmit` — clean
- `npx jest __tests__/TaskStep.test.ts __tests__/TaskExecutor.test.ts` — 7/7 PASS
