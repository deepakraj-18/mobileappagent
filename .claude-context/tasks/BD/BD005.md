# BD005 — PresenceService

Status: PASS
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-09T16:50:00+05:30
Last Checkpoint: implementation complete
Dev Checkpoint: PresenceService + BleAnchorScanner; 5 tests

Stream: BD
Order: 5
Phase: 5 — Proactivity surface
Dependencies: BD032, SC004
Blocks: FI050
Repo: deskcompanianapp
Estimate: 2.0
Spec: hub-contract.md §8.4; plan §9-4; INDEX BD005

## Description

PresenceService: hub PRESENCE authoritative; local fallback = duty-cycled BLE scan for
user-selected anchor + RSSI/miss-debounce + manual toggle + voice sleep.

## Acceptance Criteria

- [x] Hub PRESENCE wins until staleAfterSec
- [x] Local BLE scanner interface + miss debounce (Noop + Fake in tests)
- [x] Manual HOME/AWAY + voice sleep
- [x] Emit PRESENCE_LOCAL on local change
- [x] Auto-sleep callback when AWAY
- [x] Unit tests with mock scanner + hub

## Relevant Files

- `src/proactivity/PresenceService.ts`
- `src/proactivity/BleAnchorScanner.ts`
- `__tests__/PresenceService.test.ts`

## Notes

Galaxy Watch 4 path uses `BONDED_DEVICE` mode (not MAC). Real BLE module not added — interface
ready for a later native scanner.

## Review Notes

Independently verified 2026-09-09: full-repo rerun green (see BD050 notes — same run). Read
`PresenceService.ts`: hub `PRESENCE` wins until `staleAfterSec`, falling back to the local BLE
scanner interface only once stale — matches the "hub authoritative" contract, and the Galaxy
Watch 4 caveat (bonded-device check, not MAC matching) is captured in the Notes for whoever
adds the real native scanner. **PASS.**

## Test Results

- `npx tsc --noEmit` — clean
- `npx jest __tests__/PresenceService.test.ts` — 5/5 PASS
