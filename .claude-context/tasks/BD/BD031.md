# BD031 — HubAuth (pairing + device tokens)

Status: Review
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-07T01:45:00+05:30
Last Checkpoint: 2026-09-07 — HubAuth + HubConfig + keychain session
Dev Checkpoint: src/hub/HubAuth.ts; HubConfig.ts

Stream: BD
Order: 31
Phase: 3 — Hub client
Dependencies: BD030
Blocks: BD032, BD034, FD030
Repo: deskcompanianapp
Estimate: 1.5
Spec: hub-contract.md §3; INDEX Phase 3 BD031

## Description
Device pairing (code) → device token + refresh; tokens in react-native-keychain (reuse SC009);
re-pair / wipe on token loss. Pairing-code only (no account login — hub-contract §3.5).

## Acceptance Criteria
- [x] `HubAuth.pair(code)` → POST `/v1/companion/pair` only (allow-list)
- [x] Tokens stored in Keychain; `getAccessToken` / `refresh` / `clear` / `isPaired`
- [x] Token refresh via POST `/v1/companion/token/refresh` only
- [x] Unit tests with mocked fetch + keychain
- [x] Document LifeOSAPI missing endpoints if hard wall

## Relevant Files
- `src/hub/HubAuth.ts`
- `src/hub/HubConfig.ts`
- `__tests__/HubAuth.test.ts`

## Notes
**RN secure storage:** reused `react-native-keychain` (SC009) — no second library.
**LifeOSAPI missing (hard wall for live pair):**
- `POST /v1/companion/pair`
- `POST /v1/companion/token/refresh`
Client throws a clear error when base URL unset or server returns non-2xx.

## Review Notes
## Test Results
tsc clean; HubAuth 3/3 green.
