# BD004 — VoiceSession + ScreenPowerPlugin

Status: Review
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-06T23:15:00+05:30
Last Checkpoint: 2026-09-06 — ScreenPowerModule + VoiceSession state machine
Dev Checkpoint: ScreenPowerModule.kt; src/voice/VoiceSession.ts; src/native/ScreenPower.ts

Stream: BD
Order: 4
Phase: 2 — Voice loop
Dependencies: BD003, BD013
Blocks: FI005, BD050
Repo: deskcompanianapp
Estimate: 2.5
Spec: .claude-context/tasks/INDEX.md (Phase 2 BD004)

## Description
VoiceSession state machine (wake → STT → route: hub | local-fallback | command → TTS);
voice wake/sleep intents. Includes ScreenPowerPlugin.kt (sleep = GLOBAL_ACTION_LOCK_SCREEN
or device-admin equivalent; wake = FULL_WAKE_LOCK + KEEP_SCREEN_ON). Kotlin first.
Hub route is stubbed until BD030/FI030 — do not build HubClient here.

## Acceptance Criteria
- [x] ScreenPowerPlugin.kt: sleep locks screen; wake acquires wake lock + keep-screen-on
- [x] VoiceSession: wake → STT → route → TTS with testable transitions
- [x] Voice `wake` / `sleep` commands drive ScreenPower
- [x] Hub route branch present but stubbed (no HubClient)
- [x] Unit tests with mocked STT/TTS/ScreenPower

## Relevant Files
- `android/.../ScreenPowerModule.kt` (native name `ScreenPower`)
- `src/voice/VoiceSession.ts`
- `src/native/ScreenPower.ts`
- `__tests__/VoiceSession.test.ts`

## Notes
Kotlin landed first (`ScreenPowerModule`): sleep prefers a11y `GLOBAL_ACTION_LOCK_SCREEN`
via existing `AgentAccessibilityService.pressKey("lock")`, else `DevicePolicyManager.lockNow()`
if a device-admin is active. Wake uses deprecated-but-required `FULL_WAKE_LOCK` +
`ACQUIRE_CAUSES_WAKEUP` + activity `FLAG_KEEP_SCREEN_ON`.
Hub router default returns `{ handled: false }` so local-fallback always runs until Phase 3.
STT is an injectable `SttProvider` — FI005 wires capture; tests inject transcripts.

## Review Notes
## Test Results
`npx tsc --noEmit` clean; `npx jest` 24/24 including VoiceSession 5 tests.
