# BD003 — WakeWordService (sherpa-onnx KWS)

Status: Review
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-06T22:30:00+05:30
Last Checkpoint: 2026-09-06 — WakeWordService + bundled int8 KWS + RECORD_AUDIO
Dev Checkpoint: src/voice/WakeWordService.ts; KwsAssetsModule.kt; assets/kws/*

Stream: BD
Order: 3
Phase: 2 — Voice loop
Dependencies: SC004
Blocks: BD004, FI005
Repo: deskcompanianapp
Estimate: 2.0
Spec: .claude-context/tasks/INDEX.md (Phase 2 BD003)

## Description
WakeWordService — sherpa-onnx KWS, bundled model, duty-cycled start/stop riding the FGS
(SC004), wake-event stream, tap-to-talk fallback. Engine choice (sherpa-onnx) is locked in
plan.md — do not re-open. Find/build RN binding; record path in Notes.

## Acceptance Criteria
- [x] WakeWordService start/stop duty-cycled with companion docked / FGS
- [x] Wake events emitted on keyword detect
- [x] Tap-to-talk fallback path when KWS unavailable / disabled
- [x] RECORD_AUDIO restored (was deferred from SC002 for Phase 2)
- [x] Bundled or documented KWS model assets for on-device spotting
- [x] Unit tests for service API (mock native)

## Relevant Files
- `src/voice/WakeWordService.ts`
- `android/.../KwsAssetsModule.kt`
- `android/app/src/main/assets/kws/` (int8 GigaSpeech zipformer)
- `AndroidManifest.xml` (RECORD_AUDIO)
- `__tests__/WakeWordService.test.ts`
- `scripts/fetch-kws-model.ps1`

## Notes
**RN library pick:** `@siteed/sherpa-onnx.rn` (KWS API: `init` / `acceptWaveform`) +
`react-native-live-audio-stream` for PCM16 mic chunks. Bundled model:
`sherpa-onnx-kws-zipformer-gigaspeech-3.3M-2024-01-01` **int8** under `assets/kws/`.
`KwsAssets` native module copies assets → `filesDir/kws` for filesystem paths.
Duty-cycle: `bindCompanionDutyCycle()` ↔ `CompanionModeService` (DOCKED starts FGS via SC004).
Tap-to-talk: `tapToTalk()` emits `WakeEventSource.TAP` without requiring KWS.

## Review Notes
## Test Results
`npx tsc --noEmit` clean; `npx jest` — WakeWordService 5/5 + prior suites green.
