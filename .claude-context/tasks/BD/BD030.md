# BD030 — HubClient interface + MockHubClient + hub_outbox

Status: Review
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-07T01:30:00+05:30
Last Checkpoint: 2026-09-07 — HubClient + Mock + Outbox + allow-list
Dev Checkpoint: src/hub/*

Stream: BD
Order: 30
Phase: 3 — Hub client
Dependencies: BD035, SC007
Blocks: BD031, BD032
Repo: deskcompanianapp
Estimate: 2.0
Spec: .claude-context/tasks/INDEX.md (Phase 3 BD030); hub-contract.md

## Description
`HubClient` interface + `MockHubClient` (scripted scenarios for dev/tests) + `hub_outbox`
(existing SQLite queue via OutboxDao — FIFO, ack, replay-on-reconnect, cap/drop).

## Acceptance Criteria
- [x] Typed hub frame / event / command models matching hub-contract.md
- [x] `HubClient` interface (connect, send event, on command, disconnect)
- [x] `MockHubClient` with scripted scenarios + unit tests
- [x] Outbox replay helper over LocalStore OutboxDao
- [x] Explicit LifeOSAPI hub allow-list constant (no blanket API access)

## Relevant Files
- `src/hub/allowList.ts`
- `src/hub/types.ts`
- `src/hub/HubClient.ts`
- `src/hub/MockHubClient.ts`
- `src/hub/HubOutbox.ts`
- `__tests__/HubClient.test.ts`

## Notes
**LifeOSAPI hard wall:** no `/v1/companion/*` endpoints exist yet (grep 2026-09-07).
Allow-list paths defined for whoever adds them server-side:
`PAIR`, `TOKEN_REFRESH`, `CARDS`, `CONFIG`, `EVENTS`, `STREAM`.
Reveal / Password / Finance substrings explicitly denied.

## Review Notes
## Test Results
`npx tsc --noEmit` clean; HubClient tests 3/3 green.
