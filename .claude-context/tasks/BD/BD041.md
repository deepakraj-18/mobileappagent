# BD041 — Local fallback LLM provider

Status: PASS
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-09T16:05:00+05:30
Last Checkpoint: implementation complete
Dev Checkpoint: LocalFallbackLlm + store + DegradedMode; TaskExecutor fallback path

Stream: BD
Order: 41
Phase: 4 — Agent execution via hub
Dependencies: BD040
Blocks: FD041, FI040
Repo: deskcompanianapp
Estimate: 1.0
Spec: hub-contract.md §9; INDEX BD041

## Description

Local fallback LLM: single OpenAI-compatible endpoint + key in react-native-keychain;
auto-engages when the hub is unreachable (degraded mode).

## Acceptance Criteria

- [x] Config: baseUrl, model (AsyncStorage); API key in Keychain (same lib as SC009/BD031)
- [x] chatCompletions + testConnection
- [x] Agent think fallback when hub action times out / disconnected
- [x] usedLocalFallback tagging on GOAL_FINISHED
- [x] Unit tests with mocked fetch

## Relevant Files

- `src/agent/LocalFallbackLlm.ts`
- `src/agent/LocalFallbackStore.ts`
- `src/agent/DegradedMode.ts`
- `__tests__/LocalFallbackLlm.test.ts`

## Notes

Keychain service `com.privateagent.fallback-llm` — separate from hub-auth / app-lock, same library.

## Review Notes

Independently verified 2026-09-09: full-repo rerun green (see BD040 notes). Read
`TaskExecutor.ts`'s think step directly: on hub-action timeout it falls back to
`LocalFallbackLlm` only when `LocalFallbackStore.getMeta()` reports a configured provider,
and unconditionally tags `usedLocalFallback` on `GOAL_FINISHED` so the hub side can tell which
path produced a result. Keychain service name (`com.privateagent.fallback-llm`) is namespaced
separately from hub-auth per the Notes. **PASS.**

## Test Results

- `npx tsc --noEmit` — clean
- `npx jest __tests__/LocalFallbackLlm.test.ts __tests__/TaskExecutor.test.ts` — 5/5 PASS
