# BD042 — Inbound RUN_GOAL / CANCEL_GOAL

Status: PASS
Failed Stage:
Attempts: 0
Claimed By: Cursor Agent (implementer)
Claimed At: 2026-09-09T15:50:00+05:30
Last Checkpoint: implementation complete
Dev Checkpoint: GoalCommandRouter + 4 tests

Stream: BD
Order: 42
Phase: 4 — Agent execution via hub
Dependencies: BD040
Blocks: (Phase 5 agent consumers)
Repo: deskcompanianapp
Estimate: 1.0
Spec: hub-contract.md §8.2; INDEX BD042

## Description

Inbound RUN_GOAL → hand to TaskExecutor; reply GOAL_REJECTED
(ACCESSIBILITY_OFF / BUSY / UNSUPPORTED) or begin the agent loop; CANCEL_GOAL aborts it.

## Acceptance Criteria

- [x] RUN_GOAL starts executor when a11y on and idle
- [x] GOAL_REJECTED for ACCESSIBILITY_OFF / BUSY / UNSUPPORTED
- [x] CANCEL_GOAL aborts active goal → GOAL_FINISHED CANCELLED
- [x] MockHubClient unit tests

## Relevant Files

- `src/agent/GoalCommandRouter.ts`
- `__tests__/GoalCommandRouter.test.ts`

## Notes

## Review Notes

Independently verified 2026-09-09: full-repo `tsc`/`jest` rerun green (see BD040 notes — same
run). Read `GoalCommandRouter.test.ts` directly rather than trusting the "4/4 PASS" line: each
test asserts the actual `reason`/`status` field on the sent frame (`ACCESSIBILITY_OFF`,
`BUSY`, `CANCELLED`, `UNSUPPORTED` for an empty/whitespace goal) — not just that _a_ frame was
sent, which is the exact "sampling vs. enumeration" gap guard-tests.md warns about. These are
real guards. **PASS.**

## Test Results

- `npx tsc --noEmit` — clean
- `npx jest __tests__/GoalCommandRouter.test.ts` — 4/4 PASS
